# Front-End — Aplicação Mobile

> _[A definir]_ Breve descrição do projeto: contexto, objetivo do aplicativo.

## Tecnologias

- React Native
- Expo
- EAS Build (geração de APK/AAB de produção)
- TypeScript

## Base

- Aplicativo nativo (Android/iOS), desenvolvido com React Native via Expo.

---

## Descrição do Projeto

_[A definir]_ Contexto do problema que o app resolve, principais funcionalidades e fluxo geral de uso.

## Requisitos Funcionais

- **Notificação** — alertar o operador em tempo real quando um novo chamado for aberto.
- **Listagem** — exibir os chamados recebidos, com filtros e status.
- **Geolocalização** — exibir os chamados no mapa; o cálculo de proximidade/distribuição entre operadores é responsabilidade do back-end.

## Links

### Aplicação (build/deploy)

| Ambiente   | Link/Build    |
| ---------- | ------------- |
| Preview    | _[A definir]_ |
| Produção   | _[A definir]_ |

### API (Back-end)

| Ambiente | URL           |
| -------- | ------------- |
| main     | _[A definir]_ |
| develop  | _[A definir]_ |

## Guia de Contribuição

_[A definir]_ Estrutura de branches, padrão de commits, fluxo de Pull Request e regras de merge a serem alinhados com o time.

## Setup Local

_[A definir]_ Instruções de instalação de dependências, configuração de variáveis de ambiente e comandos para rodar o projeto localmente via Expo (ex: `npx expo start`).

## Scripts

_[A definir]_ Lista de comandos disponíveis, incluindo geração de build com EAS (ex: `eas build -p android --profile preview`).

## Estrutura de Pastas

_[A definir]_ Organização das pastas do projeto (components, screens, hooks, services, types, etc.), seguindo arquitetura baseada em componentes.

## Telas / Navegação

_[A definir]_ Mapeamento das telas do app (ex: login, listagem de chamados, detalhe do chamado, mapa) e fluxo de navegação entre elas.

## Integração com Back-end

_[A definir]_ Forma de comunicação com a API (REST, tempo real para notificações, etc.) e serviço utilizado para envio de push notification.

## Distribuição

_[A definir]_ Forma de distribuição do app para os operadores (APK interno via EAS Build, loja interna, TestFlight/Play Console, etc.).

## Observações Gerais

_[A definir]_ Pontos de atenção do time (permissões de câmera/localização/notificação exigidas, dependências do back-end, etc.).
